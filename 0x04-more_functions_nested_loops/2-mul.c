#include "main.h"
/**
 * mul - multiplies two integers
 * @a: first num to multiply
 * @b: second num to multiply
 * Return: thr result of multiplication between a and b
*/
int mul(int a, int b)
{
	return (a * b);
}
